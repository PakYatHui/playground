import { PageCard } from "@/components/page-card";

export default function AdminPage() {
  return (
    <PageCard
      title="管理页占位"
      description="这里预留给后续后台录单、报价复核和轻量运营工具。当前不加入登录、权限或复杂业务逻辑。"
    />
  );
}
