import { PageCard } from "@/components/page-card";

export default function ContactPage() {
  return (
    <PageCard
      title="联系页占位"
      description="后续这里会放联系方式、表单提交通道和服务说明。当前阶段只保留公开入口和结构位置。"
    />
  );
}
