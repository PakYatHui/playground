# Worklog 2026-04-13

> Historical note: this document records an earlier internal-workbench direction. It is kept only as history. The current repository direction has converged to the stage-2 public quote frontend, and `/ops` is no longer part of the active product scope.

## Objective

Continue the existing `repo/` Next.js system while exploring an earlier internal quotation workbench direction.

## What changed today

- Kept working inside the same `repo/` application instead of creating a separate landing-page shell.
- Promoted `半日陪同版` into the shared product and pricing flow:
  - public product display still comes from shared `standardProducts`
  - at that time, the temporary `/ops` workbench recorded half-day intent, task summary, estimated duration, actual duration, actual km, reimbursables, and task intensity
  - pricing logic now treats half-day companion as a first-class standard product instead of falling back to generic custom handling
- Locked the half-day default rules in `src/config/pricing.ts`:
  - included duration `240` minutes
  - overtime unit `30` minutes with round-up behavior
  - forced-custom boundary for outer route, high intensity, extra addresses over `2`, actual km over `100`, detour km over `20`, or duration over `300` minutes
- Continued and closed the `一日定制版` product landing:
  - confirmed config, pricing engine, home-page product display, and the temporary `/ops` workbench all treated it as a first-class product
  - kept pricing parameters centralized in `src/config/pricing.ts`
  - added a repeatable browser validation script for that temporary internal flow
- Refreshed delivery documentation so the repository matches the current executable product direction rather than the old scaffold description.

## Validation completed

- `npm run lint`
- `npm run build`
- Playwright browser scenarios for half-day and one-day custom quotation paths
- browser validation for the temporary internal flow

## Public access and operations

- Re-verified the formal public entry as `https://manager.pakagent.dpdns.org`
- Confirmed public HTTP `200` responses for `/` and the then-existing `/ops`
- Browser verification confirmed the real site content was being served instead of the unrelated OpenClaw control UI
- Fixed the public routing issue by pointing Cloudflare Tunnel ingress to the Next.js site on `127.0.0.1:3000`
- Added a user service for the site process so the public entry is more stable across restarts

## Result

By the end of the day, the repository held a usable public-facing service page, an internal quotation workbench prototype, executable pricing rules for airport, half-day, and one-day scenarios, and repeatable validation coverage for the delivered flows.
